# Run Demo

Run the local demo with:

```bash
python src/main.py
```

The script reads sample intake data from `sample/intake_form.json` and prints a clean owner summary.

## Output includes

- customer name
- request type
- preferred time
- status
- short summary

## Note

This is a local prototype demo. Important actions should be reviewed by a human operator.
